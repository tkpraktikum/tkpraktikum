language: node_js
node_js:
  - "5"
services:
  - postgresql
before_install:
  - psql -c 'create database tk;' -U postgres
  - psql -c 'ALTER USER Postgres WITH PASSWORD \'root\';' -U postgres
  - npm i -g strongloop
addons:
  hosts:
    - postgres
notifications:
  email: false
